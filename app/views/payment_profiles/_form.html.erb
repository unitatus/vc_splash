<%= form_for(@profile, :url => @profile.new_record? ? "/payment_profiles" : "/payment_profiles/#{@profile.id}", :html => { :class => "registration", :onsubmit => "cf_submit.disabled=true;" }) do |f| %>
<%= render :partial => 'form_cc_guts', :locals => { :f => f, :profile => @profile } %>
<div style="text-align: right; margin-right: 150px"><br>
		<%= f.submit "Save", :class => "button", :id => "cf_submit", :onclick => "return formMonitor();" %>
</div>
</div>
<br><br>
<% end %>